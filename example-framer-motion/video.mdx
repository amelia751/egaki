---
fps: 30
bpm: 120
---

import {
  TextReveal,
  TypewriterLine,
  TextRoll,
  FloatingCards,
  ArchitectureDiagram,
  MetricCounters,
  AnimatedLogo,
  GradientMorphBar,
  OrbitAnimation,
  FlipReveal,
  PulseWave,
  StatBars,
  NotificationStack,
} from './components'

# Title duration=3s

<Background>
<div style={{ position: 'absolute', inset: 0, background: '#0f0f23' }} />
</Background>

<TextReveal text="Motion + Egaki" color="#e2e8f0" />

# TextRoll duration=4s

<Background>
<div style={{ position: 'absolute', inset: 0, background: '#0a0a1a' }} />
</Background>

<TextRoll
  style={{
    fontSize: 72,
    fontWeight: 800,
    color: '#a5b4fc',
    letterSpacing: '-0.03em',
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", sans-serif',
    textAlign: 'center',
    display: 'block',
  }}
  duration={0.6}
  getEnterDelay={(i) => i * 0.06}
  getExitDelay={(i) => i * 0.06 + 0.3}
>Frame by Frame</TextRoll>

# Cards duration=4s

<Background>
<div style={{ position: 'absolute', inset: 0, background: '#0f0f1e' }} />
</Background>

<FloatingCards />

# Architecture duration=5s

<Background>
<div style={{ position: 'absolute', inset: 0, background: '#0a0f1a' }} />
</Background>

<ArchitectureDiagram />

# Metrics duration=4s

<Background>
<div style={{ position: 'absolute', inset: 0, background: '#0f0a1a' }} />
</Background>

<MetricCounters />

# Logo duration=4s

<Background>
<div style={{ position: 'absolute', inset: 0, background: '#0a0a20' }} />
</Background>

<AnimatedLogo />

# Gradient duration=5s

<Background>
<div style={{ position: 'absolute', inset: 0, background: '#0a0a15' }} />
</Background>

<GradientMorphBar />

# Orbit duration=3s

<Background>
<div style={{ position: 'absolute', inset: 0, background: '#0a0f1e' }} />
</Background>

<OrbitAnimation />

# Flip duration=4s

<Background>
<div style={{ position: 'absolute', inset: 0, background: '#0a0a1a' }} />
</Background>

<FlipReveal />

# Pulse duration=4s

<Background>
<div style={{ position: 'absolute', inset: 0, background: '#080818' }} />
</Background>

<PulseWave />

# Stats duration=5s

<Background>
<div style={{ position: 'absolute', inset: 0, background: '#0a0f15' }} />
</Background>

<StatBars />

# Notifications duration=4s

<Background>
<div style={{ position: 'absolute', inset: 0, background: '#0a0a18' }} />
</Background>

<NotificationStack />
