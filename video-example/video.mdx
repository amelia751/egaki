---
fps: 30
bpm: 120
---

import { FeatureGrid } from './components'
import { CREATE_LINES, DEPLOY_LINES, DOCS_JSON_CODE, FEATURES } from './data'
import { AsyncStats } from './async-stats'
import { TextToSpeech } from 'egaki/text-to-speech'
import Tagline from './tagline.mdx'

<Audio src="/minecraft-soundtrack.mp3" />

# Layout A duration=0.7s

<div style={{ display: '', flexDirection: 'row', alignItems: 'baseline', justifyContent: 'center', gap: 30 }}>
  <LayoutTransition id="hero">
    <span style={{ fontSize: 160, fontWeight: 700, color: '#fafafa', letterSpacing: '-0.04em', fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", sans-serif' }}>egaki</span>
  </LayoutTransition>
</div>

# Layout B duration=2s

<div style={{  display: 'flex', flexDirection: 'row', alignItems: 'baseline', justifyContent: 'center', gap: 30 }}>
  <LayoutTransition id="hero" duration={20}>
    <span style={{ fontSize: 160, fontWeight: 700, color: '#fafafa', letterSpacing: '-0.04em', fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", sans-serif' }}>egaki</span>
  </LayoutTransition>
  <SlideIn duration={20} from="right" distance={300}>
    <span style={{ fontSize: 160, fontWeight: 700, color: '#818cf8', letterSpacing: '-0.04em', fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", sans-serif' }}>video</span>
  </SlideIn>
</div>

# Opening duration=3.3s

<Background>
<MeshGradientBg colors={['#6366f1', '#8b5cf6', '#06b6d4', '#3b82f6']} speed={0.3} />
</Background>

<BlurIn duration={30}>
  <BlurReveal text="Holocron" />
</BlurIn>

# Arrakis duration=6s

<Video  src="/arrakis.mp4" style={{ width: '100%', height: '100%', objectFit: 'cover' }} />

# Tagline duration=2.8s

some text

## another duration=2s

<Background>
<MeshGradientBg colors={['#6366f1', '#8b5cf6', '#06b6d4', '#3b82f6']} speed={0.3} />
</Background>

<Tagline />

# Create duration=9.5s

<Background>
<MeshGradientBg colors={['#4f46e5', '#7c3aed', '#0891b2', '#2563eb']} speed={0.2} blur={120} />
</Background>

<FadeIn duration={15}>
  <FadeOut duration={15}>
    <TerminalSimulator lines={CREATE_LINES} title="~/projects" />
  </FadeOut>
</FadeIn>

# Configure duration=6.7s

<Background>
<MeshGradientBg colors={['#7c3aed', '#6366f1', '#2dd4bf', '#3b82f6']} speed={0.2} blur={120} />
</Background>

<FadeIn duration={12}>
  <FadeOut duration={15}>
    <GlassCodeBlock code={DOCS_JSON_CODE} title="docs.json" width={780} height={520} fontSize={16} staggerFrames={3} />
  </FadeOut>
</FadeIn>

# Features duration=5.3s

<Background>
<MeshGradientBg colors={['#8b5cf6', '#06b6d4', '#6366f1', '#ec4899']} speed={0.25} blur={120} />
</Background>

<FadeIn duration={12}>
  <FadeOut duration={15}>
    <FeatureGrid features={FEATURES} />
  </FadeOut>
</FadeIn>

```
some text
```

# Analytics duration=2s

<FadeIn duration={15}>
  <FadeOut duration={15}>
    <Server>
      <AsyncStats />
      <TextToSpeech text="Analyticsz that build themselves." />
    </Server>
  </FadeOut>
</FadeIn>

# Deploy duration=6.7s

<Background>
<MeshGradientBg colors={['#059669', '#0891b2', '#6366f1', '#2563eb']} speed={0.2} blur={120} />
</Background>

<FadeIn duration={15}>
  <FadeOut duration={15}>
    <TerminalSimulator lines={DEPLOY_LINES} title="~/my-docs" />
  </FadeOut>
</FadeIn>

# Closing duration=3.3s

<Background>
<MeshGradientBg colors={['#6366f1', '#8b5cf6', '#06b6d4', '#3b82f6']} speed={0.3} />
</Background>

<FadeIn duration={15}>
  <ShimmerSweep text="egaki" fontSize={88} fontWeight={700} baseColor="#3f3f46" shineColor="#fafafa" />
</FadeIn>
