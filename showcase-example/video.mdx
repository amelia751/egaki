---
fps: 30
bpm: 120
---

import { CardTotem, FilmStrip, ShowcaseStream, OrbitCarousel, PhotoOrbit, WheelCarousel, CarouselFlow, TickerLoop, ColumnDrift, GridReveal, SpotlightZoom, FlipGrid, PopGrid, CenterStage, FocusShift, DeckPeel, ZoomParallax, DiagonalWipe, StripeReveal, SplitReveal, StackSlide, CascadeDrop, PosterBurst, ImageTrail, PositionDance } from './components'

# Card Totem duration=4s

<Background>
  <CardTotem />
</Background>

# Film Strip duration=4s

<Background>
  <FilmStrip />
</Background>

# Carousel Flow duration=4s

<Background>
  <CarouselFlow />
</Background>

# Orbit Carousel duration=4s

<Background>
  <OrbitCarousel />
</Background>

# Showcase Stream duration=4s

<Background>
  <ShowcaseStream />
</Background>

# Photo Orbit duration=4s

<Background>
  <PhotoOrbit />
</Background>

# Wheel Carousel duration=4s

<Background>
  <WheelCarousel />
</Background>

# Ticker Loop duration=4s

<Background>
  <TickerLoop />
</Background>

# Column Drift duration=4s

<Background>
  <ColumnDrift />
</Background>

# Grid Reveal duration=4s

<Background>
  <GridReveal />
</Background>

# Spotlight Zoom duration=4s

<Background>
  <SpotlightZoom />
</Background>

# Flip Grid duration=4s

<Background>
  <FlipGrid />
</Background>

# Pop Grid duration=4s

<Background>
  <PopGrid />
</Background>

# Center Stage duration=4s

<Background>
  <CenterStage />
</Background>

# Focus Shift duration=4s

<Background>
  <FocusShift />
</Background>

# Deck Peel duration=4s

<Background>
  <DeckPeel />
</Background>

# Zoom Parallax duration=4s

<Background>
  <ZoomParallax />
</Background>

# Diagonal Wipe duration=4s

<Background>
  <DiagonalWipe />
</Background>

# Stripe Reveal duration=4s

<Background>
  <StripeReveal />
</Background>

# Split Reveal duration=4s

<Background>
  <SplitReveal />
</Background>

# Stack Slide duration=4s

<Background>
  <StackSlide />
</Background>

# Cascade Drop duration=4s

<Background>
  <CascadeDrop />
</Background>

# Poster Burst duration=4s

<Background>
  <PosterBurst />
</Background>

# Image Trail duration=4s

<Background>
  <ImageTrail />
</Background>

# Position Dance duration=4s

<Background>
  <PositionDance />
</Background>
